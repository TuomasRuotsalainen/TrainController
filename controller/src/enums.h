#ifndef ENUMS_H
#define ENUMS_H

// this is super simple at the moment, will be a proper class later
enum Command {
    LOCOMOTIVE_AHEAD,
    LOCOMOTIVE_REVERSE,
    LOCOMOTIVE_STOP,
    DECOUPLING_ACTION,
    LED
};

#endif